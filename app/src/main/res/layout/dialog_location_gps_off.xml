<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp"
    android:background="?attr/glyphSurface">

    <!-- Icon + Title row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingBottom="8dp">

        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_location_on"
            app:tint="?attr/glyphSendButton"
            android:contentDescription="@null" />

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:paddingStart="12dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Location services are off"
                android:textAppearance="?attr/textAppearanceHeadline6"
                android:textColor="?attr/glyphTextPrimary" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Required for live location sharing"
                android:textAppearance="?attr/textAppearanceBody2"
                android:textColor="?attr/glyphTextSecondary"
                android:paddingTop="2dp" />
        </LinearLayout>
    </LinearLayout>

    <!-- Message body -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Location services are turned off. Please enable location services in your device settings to share your live location."
        android:textAppearance="?attr/textAppearanceBody2"
        android:textColor="?attr/glyphTextSecondary"
        android:lineSpacingExtra="3dp"
        android:paddingTop="4dp"
        android:paddingBottom="20dp" />

    <!-- Buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="end">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_cancel"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Cancel"
            android:textColor="?attr/glyphTextSecondary"
            app:strokeColor="?attr/glyphDivider"
            android:layout_marginEnd="8dp" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_turn_on"
            style="?attr/materialButtonStyle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:icon="@drawable/ic_settings"
            app:iconGravity="textStart"
            android:backgroundTint="?attr/glyphSendButton"
            app:iconTint="?attr/glyphOnPrimary"
            android:text="Turn On"
            android:textColor="?attr/glyphOnPrimary" />
    </LinearLayout>
</LinearLayout>
