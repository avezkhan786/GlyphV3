<?xml version="1.0" encoding="utf-8"?>
<!-- Grid item for picker (emoji / GIF / sticker thumbnail).
     Uses ConstraintLayout with 1:1 ratio so height always equals width. -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="2dp">

    <ImageView
        android:id="@+id/ivPickerItem"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintDimensionRatio="W,1:1"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:scaleType="centerCrop"
        android:background="@drawable/bg_picker_placeholder"
        android:contentDescription="Media item" />

</androidx.constraintlayout.widget.ConstraintLayout>
